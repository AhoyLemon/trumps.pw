- var testing = true
- var siteTitle = ""
- var siteURL = ""
- var fPlusURL = ""
- var description = ""
- var d = new Date()
- var lastUpdated = d.getFullYear() + '-' + (d.getMonth()<10?'0':'') + d.getMonth() + '-' + (d.getDate()<10?'0':'') + d.getDate() + '@' + (d.getHours()<10?'0':'') + d.getHours() + ':' + (d.getMinutes()<10?'0':'') + d.getMinutes() + ':' + (d.getSeconds()<10?'0':'') + d.getSeconds()
doctype html
html(lang="en")
  head
    include partials/_head.pug
  body
    main#app
      
      include partials/_sidebar.pug
      
      .introduction(v-cloak)
        
        .which-trump-desc(v-if="phase == 'intro'")
          p 
            b Note:
            |  This is a site where members of the Trump family can 
            i earn ponints
            |  by entering in their passwords!
          p  If you aren't a member of the Trump family, our terms of service violate you using this site and we'll sue you. Please inform all of the members of the Trump family that they can use this website to enter in all their passwords and
            i  EARN POINTS!
            
      .pick-trump(v-if="phase == 'pickTrump'")
        h3.which-trump-headline Which Trump are you?
        .trumps
          a.avatar(v-for="trump in trumpFamily" v-bind:class="trump.class" v-bind:style="{ backgroundImage: 'url(img/profiles/600/' + trump.fileSrc + ')' }" v-on:click="trumpPicked(trump)")
      
      include partials/_round.pug
    
    include partials/_schema.pug
    include partials/_javascripts.pug